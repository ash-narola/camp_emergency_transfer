<!DOCTYPE html>
<html>
  <head>
    <title>CampEmergencyTransfer</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <div class="row">
        <div class="col-12 my-5 d-flex">
          <h3 class="display"><%= yield :heading %></h3>
          <%= yield :button %>
        </div>
          <%= yield %>
        <div>
    </div>
  </body>
</html>
